# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — never commit Reddit / Groq credentials
.env

# Intermediate data — only the final CSV (data/valorant_takes.csv) gets committed
data/raw/
data/labeled.jsonl
data/labels_review.csv

# Local training output (model weights/checkpoints) — regenerated by run_local.py
takemeter-model/

# Claude / working notes — not part of the submission
.claude/
CLAUDE.md
memory/
notes/

# Jupyter checkpoint dirs
.ipynb_checkpoints/

# Editor / OS cruft
.vscode/
.idea/
.DS_Store
Thumbs.db